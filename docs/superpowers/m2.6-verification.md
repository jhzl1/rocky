# M2.6 verification

Date: 2026-09-23. Branch `feat/m2.6-slash-commands` was built in its own worktree (`../rocky-worktrees/m2.6`),
in parallel with M2.7. It was merged into `development` with the user's approval on the same day.

## Automated

`swift test`, the final run before the merge:

```
✔ Test run with 204 tests in 28 suites passed after 2.807 seconds.
```

## Manual

The user tried it in the real app built from the worktree. That run found two problems, both now fixed and
recorded in the plan's "Changes during implementation":

- The popup opened at the end of its list, because it inherited the chat's `defaultScrollAnchor(.bottom)`.
- `/mcp` answered with a one-line summary. Terminal-only commands now open an embedded terminal, as in Conductor
  (`CMD-08`).

Task 9's checks with real agents are still open:

- Claude's list shows built-ins and skills, without the filtered names, and MCP prompts carry their tag.
- A skill found mid-session makes the list grow.
- OpenCode shows no hints, and `/init` runs.
- `/context` answers and ends the turn.
- The ghost hint sits in the right place.
- Esc closes the popup and leaves a running turn running.
- `CMD-08`, for all six commands:
  - the strip, Open terminal, Done and Refresh work;
  - the × on the strip and the × on the terminal both work;
  - Esc inside the embedded terminal, or inside a panel terminal, leaves the turn running.

## Known issues

- Only the final tree was built and tested. The intermediate commits were not built one by one.
