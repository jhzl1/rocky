# M2.7 GitHub Pull Request Panel Implementation Plan

> **Execution:** task by task. Tests are written with each change and run once, in Task 14 (global rule `## Tests` in `~/.claude/CLAUDE.md`). Build once, at the end of the tasks (the rule the user gave in M2.5), and commit only when the user asks; each task's commit line is the message to use then. This plan gives decisions, files, public interfaces, required behavior and the tests to write. It does not carry the code: the implementer writes it.

**Goal:** Inside a workspace, see its pull request the way Conductor shows it: a right panel whose header, tinted by the pull request's state, offers one next action, and a Checks tab with the editable title and body, Git status, deployments, checks and review comments. Creating the pull request, committing, fixing CI, resolving conflicts and answering comments go to the agent as prompts; Rocky itself reads GitHub, merges, marks ready, pulls and pushes. Each repository works with its own GitHub account, and every process of its workspaces gets that account's token.

**Visual and behavioral source of truth:** `docs/superpowers/design/2026-09-23-m2.7-github.html`, written by dev-2. Open it in a browser. Every requirement has an id (`HDR-02`, `GST-01`, `AGT-03`, `PR-05`…); the same id is on the element of the mock (button "Requirement pins"), in the requirement list on the right of that page, and in the task below that implements it. Every workspace in the mock's sidebar is in a different header state: select them. Values are at 100 % zoom. When the mock and a requirement's text disagree, the text wins. The mock's data, GitHub calls and agent replies are JavaScript stand-ins: do not port them. What the plan needs and the HTML does not say is under "Open questions", not decided here.

**Architecture:** Same package. RockyKit gains GitHub (`RemoteURL`, `GitHubRemote`, `GitHubAccounts`, `GitHubClient`, the pull request types, `MergeMethods`, `PullRequestHeader`, `AgentPrompts`, `PollSchedule`, `CILogs`), the branch's Git operations (`GitBranchService`, `LocalGitStatus`), the refresh loop (`PullRequestMonitor`), a `GH_TOKEN` layer in `WorkspaceEnvironment`, two `WorkspaceStatus` cases and one store migration. RockyUI gains the right panel (`RightPanel`, `PullRequestHeaderBar`, `ChecksTab`, `MergeButton`), the top bar toggle, a column divider, a toast, the `merged` token and two brand marks.

**Spec:** `docs/superpowers/specs/2026-09-22-rocky-design.md`, Section 1 (energy) and Section 2 (GitHub account per repo; this milestone takes step 3's `GH_TOKEN` and step 4, the rest of step 3 stays in M4). M2.5, the shell this builds on: `docs/superpowers/design/2026-09-23-sidebar-topbar.html` and `docs/superpowers/plans/2026-09-23-m2.5-sidebar-topbar.md`, including its "Changes during implementation". M3 (`docs/superpowers/plans/2026-09-23-m3-review-edit-pr.md`) depends on this milestone: its Changes tab (`CHG-01`) joins this panel's tab row, and its Git errors are `ERR-02`, so `ERR-01` is M2.7's alone.

## Preconditions

- M2.5 is finished and merged into `development`. Create `feat/m2.7-github` from `development` after that. Never commit on `development`. The Rocky repo has no remote and is never pushed.
- This plan was written against the M2.5 working tree of 2026-09-23, before its commit. Before Task 1, re-read these symbols and adjust the names below if they moved. **Symbols are authoritative, line numbers are not.**
  - `AppModel`: `environment(for:)` (synchronous; builds every process's environment through `WorkspaceEnvironment.make`), `makeChat` (awaits `launchEnvironment`, sets `chat.onAttention`), `attention(_:workspaceId:)` (returns early while the user watches the workspace), `onAlert`, `status(workspaceId:)`, `isWindowActive`, `selectedWorkspaceId` (`didSet`), `existingChat(workspaceId:)`, `showConversations(workspace:)`, `selectedFiles`, `createWorkspace(repoId:)`, `removeWorkspace(id:skipArchive:)`, `existingProcesses(for:)`, `openTerminal(workspaceId:)` (synchronous today), `startRun`, `startScript`, `setClaudeConfigDir` (stops Claude chats; the account setter must not), `RockyPaths`.
  - `WorkspaceEnvironment.make(login:workspace:repoVariables:claudeConfigDir:)`, `WorkspaceContext.branchName(fromBaseRef:)`.
  - `ChatSessionModel`: `send(_:attachments:)`, `queue`, `enqueue`, `sendQueuedNow`, `state`, `failure`, `onAttention`; `ChatAttention` (`finished`, `failed`, `needsYou`); `PromptAttachment.make(for:imagesAllowed:)` (files go as `resource_link`).
  - `WorkspaceStatus` with `resolve(needsYou:failure:working:unread:)`, `mostUrgent(_:)` and its private `rank`; `WorkspaceStatusGlyph` and `accessibilityName` in `SidebarRow.swift`.
  - `WorkspaceDetailView` (`topBar`, `openMenu`, `runButton`, `split`, `chatArea`; the ports now live in the Open menu), `ConversationTabs`, `PanelDivider`, `WorkspacePanelView` (calls `openTerminal`).
  - `RockyStore.migrator` (v1–v6 exist; v6 is `repo.linkedPaths`), `update(_: Workspace)` (whole-row), `Repo`, `Workspace`, `ProcessRunner.run` (an enum's static function, not an injectable instance), `ProcessFailure`, `WorktreeService` (its `GIT_TERMINAL_PROMPT=0` and `ssh -o BatchMode=yes` environment), `WorktreeService.baseRef(repo:)`, `AgentLauncher.resolve(_:path:)`.
  - `RepoSettingsView` (a `.sheet` from `SidebarView`), `SettingsContent`, `MenuButton`, `MenuItem`, `MenuDivider`, `MenuPresenter`, `MenuHost` (overlaid on `RootView` only), `Theme` tokens and `Theme.Motion`, `RockyIconButtonStyle`, `RockyTextButtonStyle`, `RockyFilledButtonStyle`, `.clickable()`, `WindowMetrics`, `Zoom.shared(_:)`, `Font.rocky`, `ShimmerText`, `CircularProgress`, `WorktreeLinker`; for tests, `GitFixture` (`localRepo`, `clonedRepo`), `Fixtures`, `FakeACP`.

## Decisions

Taken with the user on 2026-09-23:

- **M2.7 is its own milestone and lands before M3.**
- **The agent creates and fixes the pull request, as in Conductor.** "El problema no es quien crea el PR, sino la visualización": Rocky's job is to show the pull request and to hand the agent the right prompt.
- **Scope:** pull request state, checks and merge; fix CI, resolve conflicts and send GitHub review comments to the agent. Notifications are not part of M2.7 (M2.5 built them).
- **The repository's account reaches agents, terminals and scripts now** (`ENV-01`, moved from M4).
- **All GitHub reads over `URLSession`** on Rocky's schedule, never a `gh` process per refresh (`PR-07`).
- **Merge offers the methods the repository allows** (`PR-05`; Conductor uses only the first). A pick lasts for the workspace until Rocky quits and is never stored.
- **Agent buttons are disabled while the selected conversation's turn runs** ("The agent is working"), and never go into M2.5's message queue (`AGT-00`).

Taken in this plan. Change them here, not during implementation:

- **`PullRequestMonitor` owns the refresh loop,** apart from `AppModel`, with an injected loader, clock and sleep so the schedule is testable. `AppModel` forwards its triggers and builds the loader.
- **The base** is the pull request's `baseRefName` once one exists, else the workspace's `baseRef` as a branch name (`WorkspaceContext.branchName(fromBaseRef:)`). It fills every "development" of the prompts, the compare query and the compare URL (no `baseRef`: Open question 9).
- **Lookups use the live branch** (`# branch.head` of the status), not `Workspace.branch`, so an agent that renames the branch keeps its pull request.
- **The local `git status` runs on PR-07's event triggers only** (selection, key window, a Rocky action, a turn's end, the refresh button), never on the schedule's ticks: spec Section 1 forbids polling Git.
- **Without an upstream,** "ahead" counts the commits on no `origin` branch and "behind" is 0, so GST-03's `push -u origin HEAD` has a count to show.
- **`environment(for:)` stays synchronous** and reads the cached token. Every place that starts a process awaits `AppModel.prepareEnvironment(for:)` first; `openTerminal` becomes `async` for it.
- **Rocky's own `gh` runs** (`gh auth status --json hosts`, `gh auth token --user`) get the login environment without `GH_TOKEN` and `GITHUB_TOKEN`, or `gh` would report the variable's account instead of the keyring's. Never `--show-token`.
- **SSH alias hosts** (`github-celes`) are resolved once per repository per launch with `ssh -G <host>`; the remote counts as GitHub when its `hostname` is `github.com`.
- **One ephemeral `URLSession`** for GitHub: no response or credential reaches the disk cache.
- **The pull request columns are saved by a column-only update,** never `update(_: Workspace)` from a stale copy, which would undo a rename.
- **"Added to chat" marks live in memory;** hidden comment ids are stored (`PR-07` lists only those). Unsaved title and body drafts live in the monitor, per workspace, in memory.
- **A toast** is new in RockyUI (`GST-02`, `ERR-01`, `SET-01` need one), drawn as the mock's `.toast`.
- **The account picker is Rocky's menu.** The repository settings sheet is its own window, so it gets its own `MenuPresenter` and `MenuHost`; its existing `Picker`s stay as they are.
- **The tab row is a list of `RightPanelTab`** with one case in M2.7; M3 adds `.changes` (`CHG-01`) before `.checks`. The selected tab per workspace is kept in `AppModel`, in memory.
- **The panel's width and range do not zoom,** like the terminal panel's heights (M2.5 record).
- **Out of M2.7** (`OUT-30`): a Rocky form to create the pull request, the Changes tab (M3), All files, Conductor's Desktop, Summary and Review, Automerge, Approve and merge queue actions, "Continue" after merge, Vercel's "View logs", resolving or editing review threads (not planned), `GIT_AUTHOR_*`, SSH keys and the Claude instance per account (M4).

## Open questions

Each blocks the task named; the proposal is what the implementer does if nobody answers before then.

1. **`queued_to_merge`** (`HDR-02`, Task 6): does an enabled `autoMergeRequest` count, or only a `mergeQueueEntry`? And the full text of "… blocked" and "… locked". Proposal: `mergeQueueEntry` only; `AWAITING_CHECKS` → "Merge queue checks pending", `UNMERGEABLE` → "Merge queue blocked", `LOCKED` → "Merge queue locked", else "Queued to merge".
2. **"N optional checks failed"** (`HDR-02`, Task 6): `checks_failing` comes first, so any failed check wins and this label never shows, unless `checks_failing` counts required checks only (`CheckRun.isRequired`, not in `PR-01`'s query). Proposal: keep the order and show the label only when `mergeStateStatus` is `UNSTABLE` with no failed check in the list.
3. **`mergeStateStatus` `BEHIND` and `HAS_HOOKS`** (`HDR-02`, Task 6): the mock sends both to `unable_to_merge`. Proposal: `HAS_HOOKS` is `ready_to_merge`, `BEHIND` is `unable_to_merge`.
4. **"Incompatible with remote"** (`HDR-02`, `GST-01`, `AGT-06`, Task 5): no detection rule, and no rule for when the label reads "Remote branch was rebased". Proposal: incompatible when ahead and behind the upstream are both above 0; "Remote branch was rebased" when the upstream's reflog shows a forced update; otherwise "Incompatible with remote".
5. **"PR info unavailable"** (`ERR-01`, Task 7): which condition. Proposal: origin is not a GitHub remote, or GitHub answers `NOT_FOUND` for the repository.
6. **"View checks"** (`HDR-02`, Task 10): what it does. Proposal: opens `<pull request url>/checks` in the browser.
7. **Fix errors' extra text** (`AGT-03`, Task 6): the format of the annotation lines and of other CI systems' URL lines. Proposal: after a blank line, `<check name>: <annotation message>` or `<check name>: <url>`, one per line.
8. **Comments in the prompt** (`REV-01`, `AGT-05`, Task 11): a thread's replies are not in the `AGT-05` format, and a "changes requested" review with a body but no threads gives an empty prompt (`reviews` is not queried). Proposal: a thread's first comment only; the review body question goes to dev-2.
9. **A workspace without `baseRef`** (created before M2, Task 7): no base for the prompts, the compare query or the compare URL. Proposal: add `defaultBranchRef { name }` to `PR-01`'s query and use it.
10. **Layer order** (`ENV-01`, Task 4): `ENV-01` puts the account's token before repository variables (a repository variable `GH_TOKEN` wins); spec Section 3 puts account variables last. This plan follows `ENV-01`; the user confirms.
11. **Where `SET-01` goes in Settings** (Task 12). Proposal: a new "GitHub" section after "Terminal".
12. **The account picker's default entry** (`ACC-01`, Task 4): the copy for "no explicit choice". Proposal: "Automatic (jhzl1)", naming the login the default resolves to.
13. **Archive from the header** (`PR-06`, Task 12): the sidebar's Remove asks for confirmation; the header's Archive does not say. Proposal: no confirmation (merged, branch kept).
14. **Git status of a merged pull request** (`GST-01`, Task 9): the mock shows "Merged into development"; the text's fallback is "Up to date with development". The plan follows the text.

## Global constraints

- macOS 15 minimum, Swift 6 language mode. No new Swift package. The GitHub and Vercel marks are Simple Icons SVGs (CC0), like the agents' logos.
- **Zoom:** text through `Font.rocky`, text- and icon-holding sizes through `Zoom.shared(_:)`, colors from `Theme` tokens.
- **Energy (spec Section 1, `PR-07`):**
  - GitHub over `URLSession` GraphQL and REST with the repository account's token; never a `gh` process per refresh.
  - Tokens from `gh auth token --user <login>`, once per launch, on first need, in memory only; never stored, logged or put in a URL or an error message.
  - Polling only for the selected workspace while the window is key, on the `PollSchedule` backoff.
  - No disk-polling timers: `git` runs only on event triggers. `gh` runs only for the account list and the tokens; `git remote get-url` and `ssh -G` once per repository per launch.
- Menus: Rocky's own (`MenuButton`, `MenuItem`, `MenuDivider`); no SwiftUI `Menu` or `.contextMenu`.
- Git runs in the worktree with the workspace environment plus `GIT_TERMINAL_PROMPT=0` and `ssh -o BatchMode=yes`, off the main actor. Rocky never force-pushes, never creates a merge commit, never deletes a remote branch, never passes `--no-verify`.
- All code, comments, identifiers and UI copy in English. Commits: Conventional Commits, no AI attribution line. Shell: `bat`, `eza`, `rg`, `fd`, `sd`.
- Manual tests of the pull request flow only on throwaway personal repositories, never a work repository.

## Review Focus

1. A schedule tick spawns no process; `git` runs only on event triggers. Pinned by `PullRequestMonitorTests.scheduleTicksDoNotRunGit` and Task 14's process count.
2. A token never reaches the store, a log, a URL or an error text. Pinned by `GitHubAccountsTests.errorsNeverCarryTheToken`, `AppModelProcessTests` and review.
3. `GH_TOKEN` sits after the login shell and before repository variables. Pinned by `WorkspaceEnvironmentTests.githubTokenSitsBetweenTheLoginShellAndRepoVariables`.
4. The header follows `HDR-02`'s order exactly. Pinned by `PullRequestHeaderTests`, one test per row and the overlaps.
5. Every agent prompt has its exact text. Pinned by `AgentPromptsTests`.
6. Polling covers only the selected workspace while the window is key: 30 → 60 → 120 → 300 s, reset on change. Pinned by `PollScheduleTests` and `PullRequestMonitorTests`.
7. Rocky never force-pushes or creates a merge commit. Pinned by `GitBranchServiceTests`, and `rg -n '"--force|"--no-ff|"--no-verify' Sources/RockyKit/Git` finds nothing.
8. A refresh never overwrites unsaved title or body text. Pinned by `PullRequestMonitorTests.refreshKeepsDrafts`.

## File structure

```
Sources/RockyKit/GitHub/RemoteURL.swift              new: RemoteURL, GitHubRemote, GitHubRepository
Sources/RockyKit/GitHub/GitHubAccounts.swift         new: logins, active login, token cache
Sources/RockyKit/GitHub/PullRequestInfo.swift        new: snapshot, checks, deployments, comments, MergeMethods
Sources/RockyKit/GitHub/GitHubClient.swift           new: GraphQL and REST
Sources/RockyKit/GitHub/PullRequestHeader.swift      new: HeaderState, group, label, action
Sources/RockyKit/GitHub/AgentPrompts.swift           new
Sources/RockyKit/GitHub/PollSchedule.swift           new
Sources/RockyKit/GitHub/CILogs.swift                 new: tail, file names, folder
Sources/RockyKit/Git/GitBranchService.swift          new: LocalGitStatus, fetch, pull, push, fast-forward
Sources/RockyKit/App/PullRequestMonitor.swift        new: per-workspace panel state, triggers, schedule
Sources/RockyKit/App/AppModel.swift                  accounts, token, agent actions, monitor wiring, merge, archive on merge
Sources/RockyKit/App/WorkspaceStatus.swift           pullRequest(tone:), merged
Sources/RockyKit/Environment/WorkspaceEnvironment.swift   GH_TOKEN layer
Sources/RockyKit/Store/Records.swift                 Repo.githubLogin, Workspace PR columns, StoredPullRequest
Sources/RockyKit/Store/RockyStore.swift              the next free migration, savePullRequest
Sources/RockyUI/Theme.swift                          merged
Sources/RockyUI/PanelDivider.swift                   ColumnDivider
Sources/RockyUI/Toast.swift                          new: ToastPresenter, ToastHost
Sources/RockyUI/RightPanel.swift                     new: column, tab row, footer, GitHubPanelToggle
Sources/RockyUI/PullRequestHeaderBar.swift           new: header, PullRequestPill, MergeButton
Sources/RockyUI/ChecksTab.swift                      new: title and body, Git status, deployments, checks, comments
Sources/RockyUI/Resources/Icons/github.svg, vercel.svg   new (Simple Icons, CC0)
Sources/RockyUI/WorkspaceDetailView.swift            panel column, top bar toggle
Sources/RockyUI/WorkspacePanelView.swift             openTerminal is async
Sources/RockyUI/SidebarRow.swift                     PR glyphs
Sources/RockyUI/RepoSettingsView.swift               GitHub account, its own menu host
Sources/RockyUI/SettingsView.swift                   archive on merge
Sources/RockyUI/RootView.swift                       toast host
Sources/Rocky/RockyApp.swift                         ⌘⇧G
Tests/RockyKitTests/RemoteURLTests.swift             new
Tests/RockyKitTests/GitHubAccountsTests.swift        new
Tests/RockyKitTests/GitHubClientTests.swift          new
Tests/RockyKitTests/PullRequestInfoTests.swift       new: check states, order, dedupe, durations, MergeMethods
Tests/RockyKitTests/GitBranchServiceTests.swift      new
Tests/RockyKitTests/PullRequestHeaderTests.swift     new
Tests/RockyKitTests/AgentPromptsTests.swift          new
Tests/RockyKitTests/PollScheduleTests.swift          new
Tests/RockyKitTests/PullRequestMonitorTests.swift    new
Tests/RockyKitTests/CILogsTests.swift                new
Tests/RockyKitTests/RockyStoreTests.swift            migration, savePullRequest
Tests/RockyKitTests/WorkspaceEnvironmentTests.swift  GH_TOKEN layer
Tests/RockyKitTests/WorkspaceStatusTests.swift       new cases
Tests/RockyKitTests/AppModelTests.swift              account, agent actions, merge, archive on merge
Tests/RockyKitTests/AppModelProcessTests.swift       token in every process
```

The panel's tab row must take M3's Changes tab (`CHG-01`) later, placed before Checks, without changing `RightPanel`'s layout.

---

### Task 1: Remotes and GitHub accounts

**Requirements:** `ACC-01` (accounts, binding default, owner and name).
**Files:** `Sources/RockyKit/GitHub/RemoteURL.swift` (new), `Sources/RockyKit/GitHub/GitHubAccounts.swift` (new), `Tests/RockyKitTests/RemoteURLTests.swift` (new), `Tests/RockyKitTests/GitHubAccountsTests.swift` (new).

```
public struct RemoteURL: Equatable, Sendable {
  public let host: String; public let owner: String; public let name: String; public let isSSH: Bool
  public static func parse(_ url: String) -> RemoteURL?          // https, ssh://, git@host:o/n(.git)
}
public struct GitHubRepository: Hashable, Sendable { public let owner: String; public let name: String }
public enum GitHubRemote { public static func repository(from: RemoteURL, sshHostName: (String) throws -> String?) rethrows -> GitHubRepository? }
@MainActor public final class GitHubAccounts {
  public init(runGH: @escaping @Sendable ([String]) throws -> String)
  public func logins() async throws -> [String]                  // gh auth status --json hosts --hostname github.com
  public func activeLogin() async throws -> String?
  public func token(for login: String) async throws -> String    // gh auth token --user <login> --hostname github.com
  public func cachedToken(for login: String) -> String?
  public static func defaultLogin(owner: String, logins: [String], active: String?) -> String?
}
public enum GitHubAccountError: Error, CustomStringConvertible { case ghNotFound, notLoggedIn(String?), failed(String) }
```

It must:
- Fetch a login's token once per launch; concurrent callers share one `gh` run; a failure is not cached, so Retry (`ERR-01`) can try again after `gh auth login`.
- `defaultLogin`: the login equal to the owner (case-insensitive), else the active one.
- Accept `github.com` as is; any other SSH host is an alias, accepted when `sshHostName` (backed by `ssh -G`) answers `github.com`; an https host other than `github.com` is not GitHub.
- Never pass `--show-token`; an error's text is built from the exit status and stderr only, and suggests `gh auth login --hostname github.com`.

Tests:
- `RemoteURLTests`: https with and without `.git`; `git@github.com:o/n.git`; `ssh://git@github.com/o/n.git`; `git@github-celes:o/n.git` and `git@github.com-personal:o/n.git` resolved through a stub; a GitLab remote rejected; garbage → nil.
- `GitHubAccountsTests` with a fake `runGH`: parses a `gh auth status --json hosts` fixture with two accounts and the active flag; `tokenIsFetchedOncePerLogin`; `concurrentCallersShareOneFetch`; `defaultLoginMatchesTheOwnerElseTheActiveAccount`; `errorsNeverCarryTheToken` (the fake prints a token, then fails).

Commit `feat(kit): read github accounts and remotes`.

### Task 2: GitHub client and pull request data

**Requirements:** `PR-01`, the sources of `CHK-01`, `DEP-01` and `REV-01`, the calls of `PRB-01`, `PR-05`, `PR-08`, `CHK-02` and `AGT-03`, and `PR-05`'s Kit.
**Files:** `Sources/RockyKit/GitHub/PullRequestInfo.swift` (new), `Sources/RockyKit/GitHub/GitHubClient.swift` (new), `Tests/RockyKitTests/Fixtures/github-*.json` (new), `Tests/RockyKitTests/GitHubClientTests.swift` (new), `Tests/RockyKitTests/PullRequestInfoTests.swift` (new).

```
public enum MergeMethod: String, Codable, Sendable, CaseIterable { case squash = "SQUASH", rebase = "REBASE", merge = "MERGE" }
public struct RepositorySettings: Equatable, Sendable { id; squashMergeAllowed, rebaseMergeAllowed, mergeCommitAllowed: Bool; viewerDefaultMergeMethod: MergeMethod? }
public enum MergeMethods {
  public static func available(repository: RepositorySettings) -> [MergeMethod]            // order squash, rebase, merge
  public static func initial(available: [MergeMethod], viewerDefault: MergeMethod?) -> MergeMethod
}
public enum CheckState: String, Codable, Sendable { case running, failed, passed, pending, other }
public struct PullRequestCheck: Codable, Equatable, Sendable {
  name; state: CheckState; startedAt, completedAt: Date?; url: URL?          // detailsUrl ?? targetUrl
  checkRunId: Int?; workflowRunId: Int?                                      // nil for a StatusContext
}
public struct PullRequestDeployment: Equatable, Sendable { environment: String; state: String; url: URL? }
public struct PullRequestInfo: Equatable, Sendable {
  id, number, url, title, body, isDraft, isMerged, baseRefName, headRefName
  mergeable, mergeStateStatus, reviewDecision: String?; canBeRebased: Bool
  autoMergeEnabled: Bool; mergeQueueState: String?
  checks: [PullRequestCheck]; deployments: [PullRequestDeployment]
  var orderedChecks: [PullRequestCheck]; var latestDeployments: [PullRequestDeployment]
}
public struct PullRequestSnapshot: Equatable, Sendable { repository: RepositorySettings; pullRequest: PullRequestInfo?; baseAheadBy, baseBehindBy: Int? }
public struct PendingComment: Identifiable, Equatable, Sendable { id: String; author: String; body: String; path: String?; line: Int? }   // path nil: conversation
public enum CheckDuration { public static func text(start: Date?, end: Date?, now: Date) -> String? }
public enum GitHubError: Error { case offline, unauthorized, notFound, rateLimited(resetAt: Date?), graphQL([String]), http(Int) }
public actor GitHubClient {
  public init(session: URLSession, token: @escaping @Sendable () async throws -> String)
  public func snapshot(repository: GitHubRepository, branch: String, base: String) async throws -> PullRequestSnapshot
  public func pendingComments(repository: GitHubRepository, number: Int) async throws -> [PendingComment]
  public func updatePullRequest(id: String, title: String, body: String) async throws
  public func markReadyForReview(id: String) async throws
  public func merge(id: String, method: MergeMethod) async throws
  public func rerunFailedJobs(repository: GitHubRepository, runId: Int) async throws
  public func jobLog(repository: GitHubRepository, jobId: Int) async throws -> URL           // a temporary file
  public func annotations(repository: GitHubRepository, checkRunId: Int) async throws -> [String]
}
```

It must:
- **Verify every field, argument and mutation against GitHub's schema before writing the queries** (the list is in Known risks) and record any difference under "Changes during implementation".
- Send `PR-01`'s query as written; `REV-01`'s is a second query: threads neither resolved nor outdated, plus `comments(last: 50)`.
- Map `CheckRun` and `StatusContext` to `CheckState` with `CHK-01`'s sets exactly: `IN_PROGRESS` running; the five failures failed; `SUCCESS`, `NEUTRAL`, `SKIPPED` passed; not yet completed or `PENDING`/`EXPECTED` pending (counted by `HDR-02`, drawn as `CHK-01`'s "anything else" dot unless running); the rest other.
- `orderedChecks`: failed, then running, then the API's order. `latestDeployments`: the first per environment of the newest-first list (`DEP-01`).
- `CheckDuration`: "1h 4m", "3m", "45s"; running: "<1m" or "3m" since `startedAt`.
- POST GraphQL to `https://api.github.com/graphql`; REST to `https://api.github.com` with `Accept: application/vnd.github+json` and `X-GitHub-Api-Version`. The token goes only in the `Authorization` header, only to `api.github.com`; the job log's redirect is followed without it.
- Download a job log to a temporary file, never into memory.
- Map errors: no connection → `offline`; 401 → `unauthorized`; `NOT_FOUND` → `notFound`; 403 or 429 with rate limit headers → `rateLimited`.

Tests:
- `GitHubClientTests` with a `URLProtocol` stub: the request's query, variables and bearer header; parsing an open, a draft, a merged and a missing pull request, each check kind, deployments and the compare; one error response per `GitHubError`; the log redirect is requested without `Authorization`; each mutation's input.
- `PullRequestInfoTests`: the state mapping, the check order, the deployment dedupe, the durations; `MergeMethods` with one method allowed, a viewer default that is not allowed, and the order squash, rebase, merge.

Commit `feat(kit): talk to github for pull requests`.

### Task 3: Store the account and the last pull request

**Requirements:** `ACC-01` (binding), `PR-07` (stored), `REV-01` (Hide).
**Files:** `Sources/RockyKit/Store/Records.swift`, `Sources/RockyKit/Store/RockyStore.swift`, `Tests/RockyKitTests/RockyStoreTests.swift`.

```
Repo.githubLogin: String?                       // nil: ACC-01's default
Workspace: prNumber: Int?, prUrl: String?, prState: String?,  // "OPEN" | "DRAFT" | "MERGED"
           prHeaderState: String?, prChecks: String?,         // HeaderState raw value; JSON [PullRequestCheck]
           prUpdatedAt: Date?, prHiddenCommentIds: String?    // JSON [String], GitHub node ids
public struct StoredPullRequest: Codable, Equatable, Sendable { number: Int; url: URL; state, headerState: String; checks: [PullRequestCheck]; updatedAt: Date; hiddenCommentIds: [String] }
Workspace.storedPullRequest: StoredPullRequest? { get }
RockyStore: public func savePullRequest(_ stored: StoredPullRequest?, workspaceId: String) throws   // PR columns only; nil clears them
```

It must: add the columns in **the next free migration** (named after the last one in `RockyStore.migrator` when this task is written; migrations are numbered in the order they land), with nil for existing rows; update only the pull request columns.

Tests: `RockyStoreTests.theNextMigrationAddsTheGitHubColumns` (a database at the previous migration opens, migrates, keeps its rows), `storedPullRequestRoundTrips`, `savingAPullRequestKeepsTheWorkspaceName`.

Commit `feat(kit): store the github account and each workspace's last pull request`.

### Task 4: The repository's token for every workspace process

**Requirements:** `ENV-01`, `ACC-01` (binding, the settings picker).
**Files:** `Sources/RockyKit/Environment/WorkspaceEnvironment.swift`, `Sources/RockyKit/App/AppModel.swift`, `Sources/RockyUI/RepoSettingsView.swift`, `Sources/RockyUI/WorkspaceDetailView.swift`, `Sources/RockyUI/WorkspacePanelView.swift`, `Tests/RockyKitTests/WorkspaceEnvironmentTests.swift`, `Tests/RockyKitTests/AppModelProcessTests.swift`, `Tests/RockyKitTests/AppModelTests.swift`.

```
WorkspaceEnvironment.make(login:, workspace:, githubToken: String? = nil, repoVariables:, claudeConfigDir:)
AppModel
  public let githubAccounts: GitHubAccounts
  public func githubRepository(for repo: Repo) async -> GitHubRepository?   // git remote get-url origin, once per launch
  public func githubLogin(for repo: Repo) async -> String?                  // the setting, else the default
  public func setGitHubLogin(repoId: String, _ login: String?)
  public func prepareEnvironment(for workspace: Workspace) async            // the repo account's token, fetched once
  public func openTerminal(workspaceId: String) async -> PTYSession?        // was synchronous
```

It must:
- Layers, later wins: login shell < workspace variables < `GH_TOKEN` < repository variables (`ENV-01`; Open question 10). Without a token, the login shell's own `GH_TOKEN`, if any, passes through.
- `makeChat`, `createWorkspace` (before Setup), `removeWorkspace` (before Archive), `startRun` and `openTerminal` await `prepareEnvironment`. Selecting a workspace starts it in the background. A failed token does not block the process; the panel shows "GitHub access required".
- An account change applies to new processes; running agents, terminals and scripts keep theirs.
- `runGH`: `gh` from the login `PATH` (`AgentLauncher.resolve`), through `ProcessRunner` off the main actor, with the login environment minus `GH_TOKEN` and `GITHUB_TOKEN`.
- Repository settings get a "GitHub" section: an account menu (`MenuButton`, `MenuItem` with a check on the current one) listing the `gh auth status` logins and the default (Open question 12), the note "Restart the conversation's agent to use the new account", and `gh auth login --hostname github.com` when `gh` or an account is missing. The sheet gets its own `MenuPresenter` and `MenuHost`.

Tests:
- `WorkspaceEnvironmentTests.githubTokenSitsBetweenTheLoginShellAndRepoVariables`: the token replaces the login shell's `GH_TOKEN`, a repository variable replaces the token, nil keeps the login shell's.
- `AppModelProcessTests.terminalsScriptsAndAgentsGetTheRepoAccountToken`: two repositories bound to two logins of a fake `gh`; a terminal, a script and a fake agent launch each carry their repository's token.
- `AppModelTests.githubLoginDefaultsToTheOwnerElseTheActiveAccount`, `accountChangeDoesNotStopRunningAgents`.

Commit `feat(app): give every workspace process its repository's github token`.

### Task 5: The branch's local status and sync

**Requirements:** `GST-01` (local source), `GST-02` (the Git steps), `GST-03`.
**Files:** `Sources/RockyKit/Git/GitBranchService.swift` (new), `Tests/RockyKitTests/GitBranchServiceTests.swift` (new).

```
public struct LocalGitStatus: Codable, Equatable, Sendable {
  branch: String?; upstream: String?; uncommitted: Int; ahead: Int; behind: Int; commitsAheadOfBase: Int
  isIncompatible: Bool; remoteWasRebased: Bool                          // Open question 4
  public static func parse(porcelainV2 output: String) -> LocalGitStatus
}
public struct GitBranchService: Sendable {
  public init(environment: [String: String])
  public func status(worktree: URL, base: String) throws -> LocalGitStatus   // status --porcelain=v2 --branch; rev-list --count <base>..HEAD
  public func pull(worktree: URL) throws
  public func push(worktree: URL, hasUpstream: Bool) throws                   // git push | git push -u origin HEAD
  public func fetch(worktree: URL, branch: String) throws                     // git fetch origin <base>
  public func fastForward(worktree: URL, to ref: String) throws               // git merge --ff-only <ref>
  public func ownCommits(worktree: URL, since ref: String) throws -> Int
  public func prefersRebase(worktree: URL) -> Bool                            // git config pull.rebase
}
public enum GitBranchError: Error { case nonFastForward, failed(stderrTail: String) }
```

It must:
- Count uncommitted entries (untracked included) and ahead/behind from `# branch.ab`; without an upstream, ahead from `git rev-list --count HEAD --not --remotes=origin`.
- Report a rejected non-fast-forward push as `nonFastForward` ("The remote has new commits. Pull first."), other failures with stderr's last lines (`ERR-01`'s toast).
- Never `--force`, never a merge commit, never `--no-verify`; blocking, so callers go off the main actor.

Tests on temporary repositories (`GitFixture.clonedRepo`): counts of uncommitted, ahead, behind and ahead of base; no upstream; `parse` on a fixture with every entry type; `fastForward` refuses a diverged branch; `push -u` without an upstream; a rejected push is `nonFastForward`; `prefersRebase` reads the config.

Commit `feat(kit): read and sync a workspace branch with its remote`.

### Task 6: The header's state and the agent prompts

**Requirements:** `HDR-02`, `AGT-00` (Kit), the texts of `AGT-01`…`AGT-06` and `GST-02`, `OUT-30` (the actions that stay out).
**Files:** `Sources/RockyKit/GitHub/PullRequestHeader.swift` (new), `Sources/RockyKit/GitHub/AgentPrompts.swift` (new), `Tests/RockyKitTests/PullRequestHeaderTests.swift` (new), `Tests/RockyKitTests/AgentPromptsTests.swift` (new).

```
public enum HeaderState: String, Codable, Sendable {
  case merged, queuedToMerge, working, noChanges, createPR, commitAndPush, incompatible, pull, push, mergeConflicts,
       changesRequested, draftPR, checksFailing, checksPending, reviewRequired, blocked, loadingPR, readyToMerge, unableToMerge
}
public enum HeaderGroup: Sendable { case inSync, outOfSync, queued, merged, noPR, loading }
public enum HeaderAction: Sendable { case none, archive, queuedBadge, createPR, commitAndPush, resolveIncompatible, pull, push,
                                     resolveConflicts, addAllComments, readyForReview, fixErrors, viewChecks, merge }
public struct HeaderPresentation: Equatable, Sendable { group: HeaderGroup; label: String; action: HeaderAction; spins: Bool }
public enum PullRequestHeader {
  public static func state(pr: PullRequestInfo?, local: LocalGitStatus?, agentWorking: Bool) -> HeaderState
  public static func presentation(_ state: HeaderState, pr: PullRequestInfo?, local: LocalGitStatus?) -> HeaderPresentation
}
public enum AgentPrompts {
  static func createPullRequest(base: String, draft: Bool) -> String
  static func commitAndPush() -> String
  static func fixFailingChecks(notes: [String]) -> String
  static func resolveConflicts(base: String, rebase: Bool) -> String
  static func resolveIncompatibility() -> String
  static func reviewComments(number: Int, comments: [PendingComment]) -> String
  static func commitThenBringInBase(base: String) -> String
  static func bringInBase(base: String, rebase: Bool) -> String
}
```

It must:
- Apply `HDR-02`'s order, the first that applies winning: merged; queued (Open question 1); the selected conversation's turn running; no open or merged pull request → create PR when there are uncommitted changes or commits ahead of the base, else "No changes yet"; uncommitted; incompatible; behind the upstream; ahead of it; `mergeable` `CONFLICTING` or `DIRTY`; `CHANGES_REQUESTED`; draft; a failed check; a pending check; `BLOCKED` with `REVIEW_REQUIRED`; `BLOCKED`; `UNKNOWN`; `CLEAN` or `UNSTABLE` (Open questions 2 and 3); else unable to merge.
- Labels and groups from `HDR-02` and `ERR-01`, with "1 commit" in the singular; "N / M checks failed, K pending" drops ", K pending" at 0; checks failing has `fixErrors` when a failed check has a URL, else `viewChecks`; `spins` for working, checks pending and checking mergeability. Checks pending and review required have no action, queued a static badge (`OUT-30`).
- Build each prompt with the exact text of its requirement, the base filled in; `AGT-03`'s notes per Open question 7; `AGT-05`'s thread location as "path, line N", and "path" alone without a line.

Tests: `PullRequestHeaderTests`, one per row, the overlaps (uncommitted and conflicts → commit and push; failing checks and draft → draft), labels with 1 and N; `AgentPromptsTests`, the exact text of each prompt, both variants of `AGT-04` and `GST-02`, the draft variant of `AGT-01`, and `AGT-05`'s example verbatim.

Commit `feat(kit): derive the pull request header and the agent prompts`.

### Task 7: The refresh schedule and the monitor

**Requirements:** `PR-07`, `ERR-01` (which label for which failure), `AGT-00` (refresh after the turn).
**Files:** `Sources/RockyKit/GitHub/PollSchedule.swift` (new), `Sources/RockyKit/App/PullRequestMonitor.swift` (new), `Sources/RockyKit/App/AppModel.swift`, `Tests/RockyKitTests/PollScheduleTests.swift` (new), `Tests/RockyKitTests/PullRequestMonitorTests.swift` (new).

```
public struct PollSchedule: Sendable { public init(); public mutating func next(changed: Bool, running: Bool) -> Duration; public mutating func reset() }
public enum PanelError: Equatable, Sendable { case accessRequired, offline, unavailable, couldNotLoad(String) }
public enum RefreshReason: Sendable { case selected, windowKey, action, turnEnded, button, tick }
public struct PullRequestPanelState: Equatable, Sendable {
  snapshot: PullRequestSnapshot?; local: LocalGitStatus?; stored: StoredPullRequest?; login: String?
  isLoading: Bool; error: PanelError?; updatedAt: Date?
  comments: [PendingComment]; addedCommentIds: Set<String>; titleDraft: String?; bodyDraft: String?
  var headerState: HeaderState
}
@MainActor @Observable public final class PullRequestMonitor {
  public private(set) var panels: [String: PullRequestPanelState]
  public init(store: RockyStore, load: @escaping (_ workspaceId: String, _ includeLocal: Bool, _ comments: Bool) async throws -> (PullRequestSnapshot, LocalGitStatus?, [PendingComment]?),
              sleep: @escaping @Sendable (Duration) async throws -> Void, now: @escaping () -> Date)
  public func select(_ workspaceId: String?)
  public func windowKeyChanged(_ isKey: Bool)
  public func refresh(workspaceId: String, reason: RefreshReason) async
  public func setCommentsVisible(_ visible: Bool, workspaceId: String)
  public func setDrafts(title: String?, body: String?, workspaceId: String)
  public var onMerged: (@MainActor (String) -> Void)?
}
AppModel: public let pullRequests: PullRequestMonitor
```

It must:
- `PollSchedule`: 30, 60, 120, 300, 300 s; back to 30 after a change; 30 while checks or deployments run.
- Poll only the selected workspace, only while `isWindowActive`; cancel the wait on another selection or when the window stops being key; one refresh at a time per workspace.
- Refresh at once on selection, on the window becoming key, after a Rocky action, and when a turn of that workspace ends (`.finished` or `.failed`, taken in `AppModel.attention` before its "watching" guard). Event triggers pass `includeLocal: true`; ticks pass `false`.
- `AppModel` calls `select` and `windowKeyChanged` from the `didSet`s of `selectedWorkspaceId` and `isWindowActive`, and builds the loader: repository and login (Task 4), the client with that login's token, `GitBranchService.status` when asked, the base from Decisions, `pendingComments` when asked.
- Store a `StoredPullRequest` after each successful refresh; seed `panels` from the stored rows at load, so other workspaces and a relaunch show the last state.
- Keep the last state offline. Map errors to `ERR-01` labels: no token or `unauthorized` → "GitHub access required"; `offline` → "No internet connection"; no GitHub remote or `notFound` → "PR info unavailable" (Open question 5); the rest → "Couldn't load PR info" with its detail; the first load with nothing stored → "Loading PR info…".
- Never overwrite a draft; ask for comments only while they are visible; filter them by the stored hidden ids.
- Call `onMerged` once when a monitored pull request turns merged.

Tests: `PollScheduleTests.backsOffAndResets`, `staysAt30WhileRunning`; `PullRequestMonitorTests` with a fake loader and a manual sleep: `pollsOnlyTheSelectedWorkspace`, `pollingStopsWhenTheWindowIsNotKey`, `turnEndRefreshesAtOnce`, `scheduleTicksDoNotRunGit`, `refreshKeepsDrafts`, `offlineKeepsTheLastState`, `stateSurvivesRelaunch`, `mergeIsReportedOnce`.

Commit `feat(kit): refresh pull request state on a battery-friendly schedule`.

### Task 8: The right panel, its header, the toggle and the keyboard

**Requirements:** `PNL-01`, `PNL-02`, `PNL-03`, `HDR-01`, `HDR-03`, `PR-07` (footer), `ERR-01` (labels, details, toast), `ACC-01` (shown, errors), `KBD-03`.
**Files:** `Sources/RockyUI/Theme.swift`, `Sources/RockyUI/PanelDivider.swift`, `Sources/RockyUI/Toast.swift` (new), `Sources/RockyUI/RightPanel.swift` (new), `Sources/RockyUI/PullRequestHeaderBar.swift` (new), `Sources/RockyUI/WorkspaceDetailView.swift`, `Sources/RockyUI/RootView.swift`, `Sources/Rocky/RockyApp.swift`, `Sources/RockyKit/App/AppModel.swift`.

```
Theme.merged                                              // #B48CF2 (HDR-01)
struct ColumnDivider: View { @Binding var width: Double; let range: ClosedRange<Double> }   // PanelDivider turned 90°
@MainActor @Observable final class ToastPresenter { func show(_ text: String) }; struct ToastHost: View
enum RightPanelTab: String, CaseIterable { case checks }  // M3 adds .changes before .checks (CHG-01)
struct RightPanel: View { let model: AppModel; let workspace: Workspace }   // header, tab row, content, footer
struct PullRequestHeaderBar: View; struct PullRequestPill: View; struct GitHubPanelToggle: View
AppModel: public var rightPanelTabs: [String: RightPanelTab]              // per workspace, in memory
```

It must:
- `WorkspaceDetailView`: the top bar over a row of (tabs, conversation, terminal panel) | `ColumnDivider` | `RightPanel`. `@AppStorage("rightPanelOpen")` default true, `@AppStorage("rightPanelWidth")` 350 in 280–480; divider and panel go away together.
- The tab row per `PNL-03`, drawn from `RightPanelTab.allCases`, Checks alone.
- The header per `HDR-01`: tint and label color by group, the pill (`HDR-03`, ⌘-click to the browser), the label truncating with its full text as tooltip, one never-shrinking button (merge `success` fill, archive `merged`, others `textPrimary`, all with dark text; outline `panel` with a white 14 % border), `CircularProgress` when it spins, the 150 ms label and tint motion, none with Reduce Motion.
- The toggle per `PNL-02`, before Open, with ⌘⇧G in its tooltip.
- The footer per `PR-07`: login · "Updated 12s ago" (a 10 s `TimelineView` only while the window is key), refresh (`RockyIconButtonStyle(size: 22)`), the offline text; "No pull request" in place of the time without one, as the mock.
- `ERR-01`: the detail under the header (12, `danger`) with Retry; for access, `gh auth login --hostname github.com` in the tab, copyable.
- `ToastHost` over the window in `RootView`, like `MenuHost`, drawn as the mock's `.toast`: centered 52 pt below the window's top, `panel`, radius 8, 12 `textSecondary`, 150 ms fade, 2.4 s.
- Tell the monitor comments are visible while the panel is open (Checks is the only tab).
- ⌘⇧G as a View command, "Show GitHub Panel" / "Hide GitHub Panel"; checked against ⌘N, ⌘K, ⌘1–⌘9, ⌘J, ⌘U, ⌘,, ⇧Tab and the zoom keys (M2.6 adds none).

Commit `feat(ui): add the github panel with its header and toggle`.

### Task 9: The Checks tab

**Requirements:** `PRB-01`, `GST-01`, `GST-03`, `DEP-01`, `CHK-01`, `CHK-02`.
**Files:** `Sources/RockyUI/ChecksTab.swift` (new), `Sources/RockyUI/Resources/Icons/github.svg` (new), `Sources/RockyUI/Resources/Icons/vercel.svg` (new), `Package.swift` (the icons comment), `Sources/RockyKit/App/AppModel.swift`, `Tests/RockyKitTests/AppModelTests.swift`.

```
AppModel
  public func savePullRequestText(workspaceId: String) async      // updatePullRequest, then refresh
  public func discardPullRequestText(workspaceId: String)
  public func pullBranch(workspaceId: String) async                // GST-03
  public func pushBranch(workspaceId: String) async                // GST-03
  public func rerunFailedChecks(workspaceId: String) async         // CHK-02
```

It must:
- Order the tab: title, body, Git status, Deployments, Checks, Comments (Task 11).
- `PRB-01`: the borderless title field and the plain body that grows to 180 pt, then scrolls; Discard and Save when either differs from GitHub; the no-PR text in their place.
- `GST-01`: the rows in the table's order with their dots and ghost actions; "Up to date with <base>" with none (Open question 14); the "Ready to merge" row gets Task 12's `MergeButton`.
- `GST-03`: Pull disabled with uncommitted changes ("Commit or discard the changes first"); a rejected push says "The remote has new commits. Pull first."; other failures toast stderr's last lines; each action refreshes (`PR-07`).
- `DEP-01` and `CHK-01` rows with their icons, marks (template SVGs, as the agents' logos), names, ↗ and tooltips; running durations tick every 10 s only while the tab is on screen and the window is key; "Re-run" and "Fix errors" (Task 10) in the Checks header when any failed.
- `CHK-02`: one request per distinct workflow run of the failed check runs; status contexts ignored; then refresh.

Tests: `AppModelTests.rerunPostsOncePerWorkflowRun`, `savingTheTitleRefreshesAndClearsTheDraft` (stub client).

Commit `feat(ui): show the pull request's text, git status, deployments and checks`.

### Task 10: Actions through the agent

**Requirements:** `AGT-00`, `AGT-01`, `AGT-02`, `AGT-03`, `AGT-04`, `AGT-06`, `GST-02`.
**Files:** `Sources/RockyKit/GitHub/CILogs.swift` (new), `Sources/RockyKit/App/AppModel.swift`, `Sources/RockyUI/PullRequestHeaderBar.swift`, `Sources/RockyUI/ChecksTab.swift`, `Tests/RockyKitTests/CILogsTests.swift` (new), `Tests/RockyKitTests/AppModelTests.swift`.

```
public enum AgentActionAvailability: Equatable, Sendable { case available, working, stopped }   // "The agent is working" / "Restart the agent first"
public enum CILogs {
  public static func fileName(checkName: String) -> String         // "e2e / chromium" → "e2e-chromium.log"
  public static func tail(of file: URL, lines: Int) throws -> String  // reads from the end
}
RockyPaths.ciLogs: URL                                                // Application Support/Rocky/ci-logs; an init default keeps today's callers
AppModel
  public func agentActionAvailability(workspaceId: String) -> AgentActionAvailability
  public func sendAgentAction(workspaceId: String, text: String, attachments: [URL]) async
  public func createPullRequest(workspaceId: String, draft: Bool) async
  public func compareURL(workspaceId: String) -> URL?
  public func fixFailingChecks(workspaceId: String) async
  public func resolveConflicts(workspaceId: String) async
  public func pullFromBase(workspaceId: String) async              // GST-02
```

It must:
- `AGT-00`: one prompt to the workspace's selected conversation, starting its agent if needed (`showConversations`), and show that conversation (clear the file tab). Never queue (user decision): if a turn started meanwhile, do nothing.
- Disable the panel's agent buttons with `AGT-00`'s tooltips from `agentActionAvailability`; "View checks" per Open question 6.
- `AGT-01`: the split button; its chevron opens a Rocky menu with "Create draft PR" and "Create PR manually" (the compare URL).
- `AGT-03`: delete `ci-logs/<workspace id>`, then for each failed check run with a workflow run save the log's last 1000 lines there and attach it; a job that never started gives its annotations to the text, other CI systems a line with their URL.
- `AGT-04` by `prefersRebase`; `AGT-02` and `AGT-06` as written.
- `GST-02`'s four steps, with the fast-forward toast; Rocky never merges or force-pushes itself.
- Removing a workspace deletes its `ci-logs` folder.

Tests: `AppModelTests.agentActionGoesToTheSelectedConversationAndShowsIt` (fake agent receives the exact text), `agentActionsAreUnavailableWhileTheTurnRuns`, `fixErrorsAttachesLogsAndListsOtherChecks`, `pullFromBaseFastForwardsWhenOnlyBehind`, `pullFromBaseSendsTheAgentWhenDiverged`; `CILogsTests.keepsTheLastThousandLines`, `fileNameIsSafe`, `oldLogsAreDeletedFirst`.

Commit `feat(app): send pull request actions to the agent`.

### Task 11: Comments

**Requirements:** `REV-01`, `AGT-05`.
**Files:** `Sources/RockyUI/ChecksTab.swift`, `Sources/RockyKit/App/AppModel.swift`, `Sources/RockyKit/App/PullRequestMonitor.swift`, `Tests/RockyKitTests/AppModelTests.swift`.

```
AppModel
  public func sendComments(workspaceId: String, ids: [String]?) async   // nil: every pending comment
  public func hideComment(workspaceId: String, id: String)
```

It must:
- Show the section per `REV-01`: pending comments only, "Add all to chat" on the right, hidden when empty; rows with initials (no remote image), author, location or the body's first 50 characters, the full text as tooltip, Hide and Add to chat on hover.
- Hide stores the id with the pull request state (`savePullRequest`).
- Send `AgentPrompts.reviewComments` through `sendAgentAction` (a thread's comments per Open question 8) and mark the sent comments added.

Tests: `AppModelTests.addAllSendsEveryPendingCommentInOrder`, `hiddenCommentStaysHiddenAfterRelaunch`.

Commit `feat(ui): list pull request comments and send them to the agent`.

### Task 12: Merge, ready for review and archive

**Requirements:** `PR-05`, `PR-06`, `PR-08`, `SET-01`.
**Files:** `Sources/RockyUI/PullRequestHeaderBar.swift`, `Sources/RockyUI/ChecksTab.swift`, `Sources/RockyUI/SettingsView.swift`, `Sources/RockyKit/App/AppModel.swift`, `Tests/RockyKitTests/AppModelTests.swift`.

```
AppModel
  public func mergeMethod(workspaceId: String) -> MergeMethod?           // the menu's pick until quit (never stored), else MergeMethods.initial
  public func setMergeMethod(workspaceId: String, _ method: MergeMethod)
  public func merge(workspaceId: String) async
  public func markReadyForReview(workspaceId: String) async
  public func archiveMergedWorkspace(workspaceId: String) async          // removeWorkspace(id:)
  public var archiveOnMerge: Bool                                        // UserDefaults "archiveOnMerge", default false
struct MergeButton: View                                                 // the header's and the Git status row's
```

It must:
- Build `PR-05`: a plain button for one method, a split button for more; "Squash", "Merge", "Rebase"; the tooltip "Squash and merge into <base>"; the menu's items, second lines and check, rebase disabled with "GitHub can't rebase this branch cleanly"; "Confirm …" in `attention` for 4 s, cancelled by picking another method; "Merging…" with a spinner; errors under the header. The header and the Git status row share the method and the confirmation.
- `PR-08`: the mutation, then refresh; no confirmation.
- `PR-06`: Archive runs `removeWorkspace(id:)` (Open question 13); the remote branch stays.
- `SET-01`: the switch in `SettingsContent` (Open question 11); on, `onMerged` archives the workspace and toasts "Archived <name>".

Tests: `AppModelTests.mergeUsesThePickedMethodUntilQuit`, `archiveOnMergeIsOffByDefault`, `archiveOnMergeRemovesTheWorkspaceOnce`.

Commit `feat(ui): merge pull requests and archive merged workspaces`.

### Task 13: Pull request glyphs in the sidebar

**Requirements:** `ROW-07`.
**Files:** `Sources/RockyKit/App/WorkspaceStatus.swift`, `Sources/RockyKit/App/AppModel.swift`, `Sources/RockyUI/SidebarRow.swift`, `Tests/RockyKitTests/WorkspaceStatusTests.swift`, `Tests/RockyKitTests/AppModelTests.swift`.

```
public enum PullRequestTone: Sendable { case passed, running, failed, draft }
WorkspaceStatus: + case pullRequest(tone: PullRequestTone), merged        // after unread, before idle
WorkspaceStatus.resolve(needsYou:failure:working:unread:pullRequest: StoredPullRequest?)
```

It must: take the tone from the stored state in the mock's order (draft, then failed or conflicts, then running, else passed); rank the new cases in `mostUrgent`; draw `merged` with the `merged` glyph and a `textTertiary` title; name them "Pull request" and "Merged" for VoiceOver. Glyphs: SF Symbols `arrow.triangle.pull` and `arrow.triangle.merge` if they read like the mock's at 14 pt, else template SVGs in `Resources/Icons`.

Tests: `WorkspaceStatusTests.priorityOrder` with the new cases, `pullRequestToneOrder`; `AppModelTests.mergedWorkspaceShowsMergedStatus`.

Commit `feat(ui): show pull request state in the sidebar`.

### Task 14: Build, tests and verification

1. `swift build` and `swift test`. Fix and rerun until green.
2. `scripts/make-app.sh`, `open build/Rocky.app`, then the manual checklist with the HTML open next to the app.
3. Energy: a workspace with a pull request selected, then 10 minutes idle: Rocky starts no `gh` or `git` process (Activity Monitor or `CurrentPowerlog.PLSQL`), fewer than 5 app-spawned processes per minute.
4. Merge `feat/m2.7-github` into `development` only with the user's approval.

Manual checklist, on throwaway personal repositories only (`gh` accounts `jhzl1`, active, and `ocampos-biai`, both with ssh as the git protocol):
1. Repository settings list both logins. A throwaway repository under `jhzl1` defaults to it; one under `ocampos-biai` defaults to `ocampos-biai` although it is not the active account; picking the other account in the settings and back changes the footer each time (`ACC-01`).
2. In a terminal of the `ocampos-biai` repository, `gh api user --jq .login` prints `ocampos-biai` while `gh auth status` still marks `jhzl1` active. Change the account: the note shows; the running agent keeps the old token until restarted. `rg -ac 'gh[op]_' ~/Library/Application\ Support/Rocky/rocky.sqlite* ~/Library/Logs/Rocky` finds no token (`ENV-01`).
3. Create PR, draft and normal: the conversation gets the exact prompt, the header shows "Working…", the pull request appears when the turn ends; Create PR manually opens the compare page (`AGT-00`, `AGT-01`, `PR-07`).
4. The agent's `git push` in the `ocampos-biai` repository: `ssh -T git@github.com` from the worktree names the account its key belongs to; a push refused because the key is `jhzl1`'s is recorded, not fixed (SSH keys are M4). Optionally switch the remote to https to push with `GH_TOKEN` (`ENV-01`, `GST-03`).
5. Edit the title and body, save, discard; a refresh while editing keeps the text (`PRB-01`).
6. A failing workflow: the label, Re-run (checks run again), Fix errors (logs under `ci-logs`, the previous ones gone) (`CHK-01`, `CHK-02`, `AGT-03`). A deployment environment if the repository has one (`DEP-01`).
7. A conflicting commit on the base: "Merge conflicts" and the prompt for the repository's `pull.rebase`; the "behind" row's Pull, fast-forward and diverged; Pull and Push against the branch's remote, and a rejected push (`AGT-04`, `GST-01`…`GST-03`).
8. From the other account, a line comment, a conversation comment and "changes requested": Add to chat, Add all, Hide, relaunch (`REV-01`, `AGT-05`).
9. Draft → Ready for review (`PR-08`); allow all three methods on the throwaway repository and merge with each, through the menu and the 4 s confirmation; the pick lasts until quit (`PR-05`); Archive (`PR-06`); `SET-01` on, merge archives and toasts.
10. Polling: no request while another app is in front; a refresh on return; slower while nothing changes; other workspaces' glyphs from the stored state, also after relaunch (`PR-07`, `ROW-07`).
11. A repository with no GitHub remote, Wi-Fi off, and an account without access to the repository: each shows its `ERR-01` label; offline keeps the last state.
12. ⌘⇧G, dragging the divider, width and open state after relaunch, 150 % zoom (`PNL-01`…`PNL-03`, `HDR-01`…`HDR-03`, `KBD-03`).

## Known risks

- **GraphQL names are written from the HTML and memory.** Task 2 checks each against GitHub's schema (introspection with the user's token, or the GraphQL reference): `updatePullRequest`, `markPullRequestReadyForReview`, `mergePullRequest` and its `mergeMethod` enum, `statusCheckRollup.contexts` with `CheckRun` (`checkSuite.workflowRun.databaseId`) and `StatusContext`, `deployments(orderBy:)` with `latestStatus.environmentUrl`, `reviewThreads` (`isOutdated`, `line`), `viewerDefaultMergeMethod`, `squashMergeAllowed`, `rebaseMergeAllowed`, `mergeCommitAllowed`, `canBeRebased`, `mergeQueueEntry`, `autoMergeRequest`, and `Ref.compare(headRef:)` with which way `aheadBy` and `behindBy` count.
- **The REST rerun endpoint** (`POST …/actions/runs/{run_id}/rerun-failed-jobs`) may need a scope `gh`'s token lacks. Check `gh auth status` lists `workflow` and `repo`, and call it once on the throwaway repository before Task 9.
- **GraphQL rate limits per account** (5,000 points an hour per user, each account its own budget). Add `rateLimit { cost remaining }` to the query once during Task 2's check: at 30 s the selected workspace makes 120 queries an hour. A secondary limit shows as "Couldn't load PR info" and the schedule's next tick retries.
- **The agent's `gh` honoring `GH_TOKEN` over the keyring's active account.** M0 checked it with `git ls-remote` only. Manual item 2 settles it.
- **`git push` from the agent over ssh:** the token does not apply, the key does, and both accounts use ssh. Manual item 4 shows which key answers; a wrong one is M4's.
- **Job logs are large** (`AGT-03` saves them under `Application Support/Rocky/ci-logs`). A log goes to a temporary file and only its last 1000 lines are read, from the end; its redirect must not carry `Authorization` to the blob host (pinned by `GitHubClientTests`). Check with the biggest failing job on the throwaway repository and watch Rocky's memory.
- **⌘⇧G is the system's Find Previous** in text views (and M3's editor will have a find bar). Check the Edit menu of the running app and that ⌘⇧G in the message box toggles the panel.
- **Narrow windows:** sidebar, conversation and a 280 pt panel. Check the window's minimum width keeps the conversation readable.
- **"Behind by N" is as fresh as the last fetch:** Rocky never fetches on a timer (`GST-01`), so it changes after the agent's or `GST-02`'s fetch. Accepted.

## Changes during implementation

Record here anything that differs from this plan, with the date and the reason.
