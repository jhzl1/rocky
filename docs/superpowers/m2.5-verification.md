# M2.5 verification

Date: 2026-09-23. Branch `feat/m2.5-sidebar-topbar`, merged into `development` with the user's approval on the same
day ("Haz el merge y para adelante"), before the manual checklist.

## Automated

`swift test`, the final run (global rule: tests run last, before the merge).

- **First attempt:** it hung, and no test finished. Every cooperative thread was waiting in `ProcessRunner.run` for a
  stderr read queued on `DispatchQueue.global()`. That read never got a thread.
- **Fix:** `PipeDrain` now reads each pipe on a thread of its own. It is recorded in the plan's "Changes during
  implementation".
- **Second attempt:**

```
✔ Test run with 166 tests in 24 suites passed after 2.300 seconds.
```

## Manual checklist (Task 10)

Not run yet. Run it on `development` with the design HTML (`docs/superpowers/design/2026-09-23-sidebar-topbar.html`)
open next to the app. It covers the plan's items, plus what M2.5 built beyond its plan:
- the last session reopening
- the settings modal
- the welcome screen
- the aligned bottom bars
- environment files linked into a new workspace (veritas: `apps/api/.dev.vars`, `.env*`)
- the alert sound and the Dock badge
- the message queue: queue, send now, edit, delete, hold after Stop
- Esc stopping a turn, but not while a menu, the settings or a sheet is open
- the ports gone from the UI

Each change checked by hand during the work, in the real app, is recorded with that change in the plan.

## Not measured yet

- WIN-01's H, measured against the real title bar.
- Instruments on the spinner: the Core Animation spinner should cost Rocky no work per frame.
- Energy: `scripts/energy-report.sh 30` after 30 idle minutes.

## Known issues

- The message queue lives in memory only: quitting Rocky drops queued messages.
- Only the final tree was built and tested; the intermediate M2.5 commits were not built one by one.
