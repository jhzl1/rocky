# M1 Verification

Date: 2026-09-23. macOS 27.0, Xcode 27.0. Branch `feat/m1-workspaces-chat`.

## Automated tests

Tasks 1–11 were committed without compiling. The first `swift build` had no errors. After the fixes found in the manual checklist (below), the suite was run again:

```
✔ Test run with 62 tests in 9 suites passed after 0.512 seconds.
```

`ChatSessionModelTests` passed three runs in a row. `scripts/make-app.sh` builds `build/Rocky.app`, ad hoc signed, `Identifier=dev.jhzl.rocky`.

## Manual checklist

The checklist was run against `~/Documents/dev/rentek/doculift`, not the personal repo the plan names.

| # | Item | Result |
| --- | --- | --- |
| 1 | Add a repository | Pass: a "doculift" section appears. |
| 2 | New Workspace | Pass: `bergen` and `bogota` created; output below. |
| 3 | OpenCode chat | Not run. |
| 4 | Claude Code chat | Fail at first, then pass. With no Claude instance set, the adapter used `~/.claude`, whose login had expired. With `~/.claude-rentek` the chat answers. No permission sheet: the instance runs in `bypassPermissions` mode. |
| 5 | Claude instance from repo settings | Pass for a chat started after the change; output below. A chat already running kept the old instance (fixed, see below). |
| 6 | Quit leaves no agent process | Pass: output below. Not confirmed that an agent was running when Rocky quit. |
| 7 | Resume after reopening | Not run. |
| 8 | Remove Workspace | Refused, as designed for a dirty worktree: `bergen` held untracked `.atl/` files written by the Claude session. Folder and branch kept. |

Item 2:

```
$ eza ~/Documents/dev/rentek/doculift-worktrees
bergen
bogota
$ git -C ~/Documents/dev/rentek/doculift branch --list 'rocky/*'
+ rocky/bergen
+ rocky/bogota
```

Item 5 (the `printenv CLAUDE_CONFIG_DIR` prompt was not sent; the session file location shows which instance ran it):

```
$ fd 776bf5db ~/.claude-rentek/projects
/Users/jhzl/.claude-rentek/projects/-Users-jhzl-Documents-dev-rentek-doculift-worktrees-bogota/776bf5db-e2f9-44ef-98b5-a0e024a4f568.jsonl
```

Item 6:

```
$ pgrep -fl 'Rocky.app/Contents/MacOS/Rocky|claude-agent-acp|opencode acp'
(no output)
```

Item 8:

```
Could not remove bergen: git worktree remove /Users/jhzl/Documents/dev/rentek/doculift-worktrees/bergen exited 128: fatal: '/Users/jhzl/Documents/dev/rentek/doculift-worktrees/bergen' contains modified or untracked files, use --force to delete it
$ git -C ~/Documents/dev/rentek/doculift-worktrees/bergen status --porcelain
?? .atl/
```

## Energy at rest

Not measured: `scripts/energy-report.sh 30` was not run.

## Fixed during verification

- `1590453` fix(kit): restart claude with the new instance after a settings change. Saving a new Claude instance now stops the running Claude chats of that repo, and the workspace view shows Start again.
- `f25ce06` fix(kit): start a new session when the agent cannot resume the old one. A session created under another Claude instance no longer leaves the chat stopped.
- `4e9ce9e` fix(ui): show the repo menu as a single button. The menu chevron read as a second control.

## Known issues

- `open build/Rocky.app` failed three times in a row, then worked with no change to the file:
  `The application cannot be opened for an unexpected reason, error=Error Domain=RBSRequestErrorDomain Code=5 "Launch failed." … NSPOSIXErrorDomain Code=162 … Launchd job spawn failed`.
  launchd logged `xpcproxy exited due to OS_REASON_CODESIGNING | embedded signature doesn't match attached signature`. Cause not found.
- A Claude session writes `.atl/` into the worktree: a hook in `~/.claude/settings.json` runs `gentle-ai skill-registry refresh` (the file header says "Auto-generated by gentle-ai skill-registry refresh"). "Remove Workspace" is refused until those files are deleted.
- An agent error is shown twice: once as agent text and once as `Internal error: …`.
- Items 3 and 7 and the energy measurement are still open.
