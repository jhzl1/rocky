# M2.8 verification

Date: 2026-09-25. Branch `feat/m2.8-conversations`, from `development` at `f82cf5d`, which already had M3 and its
follow-up branch, with `CNV-01`, `CNV-02`, `KIT-13` and `KIT-11`'s `newConversation(workspace:)`. Plan:
`docs/superpowers/plans/2026-09-23-m2.8-conversations.md`. Design: `docs/superpowers/design/2026-09-23-m2.8-conversations.html`.

## Automated

Tasks 1–5 were written by one writer, and nothing was built between tasks. The one build at their end
(`swift build`, then `swift build --build-tests`) had no errors or warnings. `swift test` on that tree:

```
✔ Test run with 619 tests in 64 suites passed after 4.393 seconds.    (parallel, run 1)
✔ Test run with 619 tests in 64 suites passed after 3.719 seconds.    (parallel, run 2)
✔ Test run with 619 tests in 64 suites passed after 27.123 seconds.   (--no-parallel)
```

No test failed at any point. 599 before (M3's final run) and twenty added, in one new suite and two existing ones:
- `AgentModelCatalogTests` (new, `KIT-12`): `aRecordedListComesBackFromTheFile`, `claudeInstancesKeepTheirOwnLists`,
  `anUnchangedListLeavesTheFileUntouched`, `otherOptionsAndEmptyListsAreIgnored`, `theFileHoldsModelNamesOnly`.
- `ChatSessionModelTests`: `eachNewListOfModelsIsReported`, `aResumedSessionReportsItsModels`,
  `thePendingModelIsSetOnceWhenTheOptionsArrive`, `aPendingModelTheAgentDoesNotHaveKeepsItsDefault`,
  `afterAModelChangeTheEffortIsTheOneTheAgentAnswered`, `stoppingWhileStartingStaysStopped`.
- `AppModelTests`: `aSessionsModelsReachTheCatalog`, `claudesModelsAreKeptUnderTheRepositorysInstance`,
  `aPickInAnEmptyConversationSwitchesItInPlace`, `aSwitchStopsAnAgentThatIsStillStarting`,
  `aPickWithMessagesOpensANewConversationAndLeavesTheOldOne`, `aQueuedMessageCountsAsAMessage`,
  `aPickOfTheSameAgentSetsTheModel`, `aPickedModelTheAgentDoesNotHaveKeepsItsDefault`,
  `terminalToggleRequestsCountPerWorkspace`.

The fake agent (`Fixtures/fake-acp-agent.sh`) now keeps its model, effort and mode, answers every
`session/set_config_option` with the whole new list, offers Claude's or OpenCode's lists (`FAKE_ACP_AGENT`), and reports
its options on `session/load` too. The existing tests pass unchanged with it.

That includes the checks the Review Focus points to:
- AGM-02 never leaves the conversation without a chat, and keeps its record, tab and selection:
  `aPickInAnEmptyConversationSwitchesItInPlace` watches `chats` during the whole switch.
- AGM-03 leaves the old conversation, its agent and its session as they were, and moves the draft with its chip:
  `aPickWithMessagesOpensANewConversationAndLeavesTheOldOne`.
- The picked model is applied once, before the agent is ready, with the fallback toast: `thePendingModelIsSetOnce…`,
  `aPendingModelTheAgentDoesNotHave…`, `aPickedModelTheAgentDoesNotHave…`.
- The effort after a model change is the agent's answer: `afterAModelChangeTheEffortIsTheOneTheAgentAnswered`,
  `aPickOfTheSameAgentSetsTheModel`.

## Manual checklist (Task 6)

Not run: the app was not built with `scripts/make-app.sh` nor launched (the user had Rocky open). In the built app:

- **The rail (AGM-01):** 48 wide, the menu's full height, darker than the body with a hairline on its right; 34-point
  items 6 apart; unselected at 60 %, hovered at 85 % on `fillHover`, selected on `fillSelected` with the 3-point bar
  on the rail's left edge; the green dot on the conversation's agent, ringed in `panel`; tooltips "Claude Code
  (current)" and "OpenCode"; a click, then ↑ and ↓, move between agents; the menu opens on the conversation's agent.
  Check with `screencapture` that the rail's fill follows the menu's rounded corners and reaches its bottom.
- **A switch in an empty conversation (AGM-02):** Claude Code → OpenCode, then back. The tab keeps its place and its
  selection and only its icon changes; "Opening the conversation…" never shows; the menu stays open, the dot moves, the
  title shows the spinner, then "N models", and the Effort row grows in; the draft (text, a file badge and a line chip)
  stays in the box with the keyboard; the new agent's slash commands arrive. With OpenCode never started on the Mac
  (move `agent-models.json` away), the body says "OpenCode hasn't reported its models yet." and Start switches.
- **A pick with messages (AGM-03):** a new tab at the end, selected, with the picked model; the draft and its chip in
  its box; the old tab's box empty; the old agent still running; the menu closed. A queued message counts as a message.
- **The fallback toast:** with Rocky quit, add a model OpenCode does not have to its list in `agent-models.json`, then
  pick it in an empty Claude Code conversation: OpenCode keeps its default and the toast reads "<name> isn't available
  in OpenCode; using <its default>".
- **The effort motion (AGM-07):** with OpenCode, GPT-5.5 animates the levels in and the menu grows upwards; Qwen3 Coder
  animates the row out and the menu stays; Claude Sonnet after GPT-5.5 High shows Sonnet's default, not High; an effort
  pick slides the fill; the chips are equal width. Again with Reduce Motion: fades only, no growth, no slide.
- **The menu stays open** after a model or an effort pick, and closes on Esc, a click outside, sending, and AGM-03.
- **The button (AGM-05, AGM-07):** while the agent starts, a menu right away with the spinner and the chevron, no text,
  tooltip "Starting Claude Code"; a model picked meanwhile is checked in the list and applied when the agent is ready;
  "Opus 5.5 High", or "GPT-5.5" at "Default". Disabled during a turn (AGM-06).
- **"+" (CNV-01):** one click, no menu, no right-click menu.
- **⌃` (KBD-04):** from the message box, the panel unfolds on a terminal with the caret in it and typing goes to the
  shell; ⌃` again folds it and the caret is back in the message box. From a file tab's editor and a diff tab in Edit,
  the same, back to the editor. With no terminal, "Terminal 1" opens and takes the keyboard; with only Setup and Run, a
  terminal opens too. With the embedded terminal of `/mcp` focused, ⌃` goes to the panel. Alongside ⌘J: both fold the
  same panel. View ▸ Toggle Terminal is off with no workspace selected.

## Mini-modals (Tasks 7–9, DLG-01…DLG-06)

### Automated

Tasks 7 and 8 were written by one writer, with no build between them. The one build at their end (`swift build`,
then `swift build --build-tests`) had two errors, both fixed before any test ran: `Dialog.row` called the buttons'
main-actor `isEnabled` from a nonisolated getter (now `@MainActor`), and the quit prompt's `.cancel { … }` passed its
trailing closure to `isEnabled` instead of `action`. After that, no errors or warnings. `swift test` on the whole
branch:

```
✔ Test run with 628 tests in 65 suites passed after 4.527 seconds.    (parallel, run 1)
✔ Test run with 628 tests in 65 suites passed after 4.306 seconds.    (parallel, run 2)
✔ Test run with 628 tests in 65 suites passed after 33.428 seconds.   (--no-parallel)
```

No test failed. 620 before (Task 6's 619 and `anAgentWithNoListLoadsItsModelsByItself`, added with AGM-04's user
decision) and eight added in one new suite, `DialogButtonsTests` (`DialogButtonRow`, `PermissionRequest.buttons`):
`theUnsavedPromptPutsDontSaveAtTheLeftAndSaveLast`, `aConfirmationEndsWithItsDestructiveDefault`,
`aLoneOKIsTheDefaultAndTheCancel`, `aSecondaryButtonAtTheRightIsNoDefault`, `tabWalksTheRowFromTheDefaultAndWraps`,
`tabSkipsDisabledButtons`, `aPermissionRequestOrdersItsFourKindsAndAnUnknownOne`,
`aPermissionRequestWithoutAllowOnceHasNoDefault`.

`rg -n '\.confirmationDialog\(|\.alert\(|\.sheet\(' Sources/` finds nothing. The one `NSAlert` left is DLG-05's
no-window case (`RockyApp.swift`); the `NSOpenPanel`s stay.

### Manual checklist (Task 9)

Not run: the app was not built with `scripts/make-app.sh` nor launched. In the built app, with `screencapture` for the
look:

- **The look (DLG-02):** the window dims to black 45 %; the panel is 360 wide, centered, `rockyBackground` with the
  ring and shadow of Settings; the title 14 semibold and the message 12.5 `textSecondary`, both left-aligned; the
  buttons 28 high, 8 apart, at the right. The dim fades in, the panel grows from 0.98, and closing is quicker; with
  Reduce Motion both only fade. Two dialogs in a row (an error while a confirmation shows) show one after the other.
- **Each of the 11 dialogs** (DLG-01's table), with its text and its buttons, left to right:
  - a diff tab's Discard: Cancel · Discard Changes;
  - Changes' Discard, one file and Discard All Uncommitted Changes…: Cancel · Discard Changes, or Discard All;
  - Esc in a comment box with text: Keep Editing · Discard, and Keep Editing gives the box its caret back;
  - closing a tab with unsaved edits: Don't Save at the left · Cancel · Save;
  - Remove Workspace…: "Remove tokyo?", Cancel · Remove Worktree;
  - a failing archive script: "Archive script failed", Cancel · Remove Anyway;
  - the merged header's Archive with uncommitted changes: Cancel · Archive;
  - an error (a git failure in the model's `errorMessage`): "Something went wrong", OK, its message selectable and
    copyable with ⌘C;
  - quitting with unsaved edits: Don't Save · Cancel · Save All;
  - Commit (DLG-06): 500 wide, the files, subject, counter and description as before, the hint at the left of Cancel ·
    Commit; Commit off with an empty subject; ⌘Return commits, Return in the description types a line; while git runs,
    neither Cancel, Esc nor a click outside closes it; a failure keeps it open with git's output; a commit that works
    closes it with the "Committed" toast. In a small window, or zoomed in, the body scrolls within 80 % of the window.
  - a permission request (below).
- **Keys (DLG-03):** Return presses the rightmost button and Esc cancels (OK in the error); a click on the dim cancels.
  Tab and ⇧Tab move the accent ring around the buttons, wrapping, skipping a disabled Commit; Space presses the ringed
  button, and so does Return once the ring has moved. ⌘D is Don't Save. Behind a dialog nothing takes a key: typing,
  ⌘J, ⌃`, ⌘K, ⌘1, ⌘P, ⌘S and ⌘W do nothing, and holding ⌘ shows no sidebar hints; ⌘Q, ⌘H, ⌘M and the zoom keys work.
  The message box has no caret while a dialog shows, and gets it back after one closes.
- **Esc with a menu open over a dialog:** open the model menu, then make a permission request arrive (a Bash command
  in Claude Code with the menu left open): Esc closes the menu first, and a second Esc cancels the request.
- **Settings and Quick Open under a dialog:** an error while Settings or Quick Open is open: Esc cancels the dialog
  first, and a second Esc closes Settings or Quick Open.
- **Quitting (DLG-05)** with unsaved edits: from the window; with the window minimized; with Rocky hidden (⌘H); with
  the window on another Space; with a confirmation open, which ⌘Q cancels before the quit prompt shows; Save All that
  cannot save a file, which cancels the quit and shows its tab. With no window (⌘W, then ⌘Q from the menu bar): the
  native alert, as before.
- **A permission request from Claude Code (DLG-06):** 460 wide, Claude's mark and "Permission needed", the command in
  the mono block (selectable; a long one scrolls after ten lines); Cancel · Reject at the left, Always Allow · Allow at
  the right. Return allows once; Esc, Cancel and a click outside cancel the request. A request that arrives while
  another conversation is on screen shows when its conversation is selected, and the message box does not take the
  keyboard behind it. VoiceOver starts on the title.

## Final run before the merge (2026-09-25)

After the last change on the branch (the mini-modal's height, `HeightCap`), one parallel run and one serial run:

```
✔ Test run with 628 tests in 65 suites passed after 4.157 seconds.    (parallel)
✔ Test run with 628 tests in 65 suites passed after 30.950 seconds.   (--no-parallel)
```

## Known issues

- Only the final tree was built and tested.
- The mini-modals' look and keys were checked by reading the code only, not in the running app (above).
- `matchedGeometryEffect` in a menu that measures its own height, and the menu's growth upwards: not observed (plan's
  Known risks).
- ⌃` on a layout where ` is a dead key may not fire, as in VS Code.
